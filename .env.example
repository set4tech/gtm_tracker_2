# Database Configuration
# For local development, use PostgreSQL or SQLite
# Example PostgreSQL: postgresql://user:password@localhost:5432/gtm_tracker
# Example SQLite: sqlite:///./gtm_tracker.db

DATABASE_URL=postgresql://user:password@localhost:5432/gtm_tracker

# For Vercel Postgres:
# 1. Create a Postgres database in your Vercel dashboard
# 2. Link the database to your project
# 3. Run `vercel env pull` to get the DATABASE_URL
# 4. The DATABASE_URL will be automatically set in production

# Slack Integration Configuration
# Get these from https://api.slack.com/apps after creating your Slack app
# 1. Go to "Settings" > "Basic Information" for the Signing Secret
# 2. Go to "Settings" > "Install App" for the Bot Token

SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here

# Slack Notification Channel (optional, defaults to #all-set4)
# Set this to the channel where new activity notifications should be posted
SLACK_NOTIFICATION_CHANNEL=all-set4

# Note: These are optional for local development
# The app will work without Slack integration if these are not set
